services:
  - type: web
    name: ai-resume-analyzer
    env: docker
    plan: free
    region: oregon
    branch: main
    numInstances: 1
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
